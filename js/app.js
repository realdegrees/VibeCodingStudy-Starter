const app = {
    init() {
        // Initialisierung
    },
    
    fetchWeather(city) {
        // API-Call
    },
    
    displayWeather(data) {
        // UI Update
    }
};

// Start app when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    app.init();
});